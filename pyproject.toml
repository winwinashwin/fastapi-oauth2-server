[project]
name = "fastapi-oauth2-server"
version = "0.1.0"
description = "Example FastAPI OAuth 2 Server w/ Authlib."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["authlib>=1.6.5", "flask>=3.1.2", "flask-SQLAlchemy>=3.1.1"]

[dependency-groups]
dev = ["prek", "ruff"]
